#include <string>

using namespace std;

// Initializing files here

std::string DecreaseTreeSizeAndCuts(char filename[], char dirname[], char decsize[]);


std::string DmHist(const char* filename);

std::string Fit(const char* filename);
